// Projectile
// -------------------------------------------------- //

(function(TK) {
    
    var Projectile = TK.Unit.extend({
        
        initialize: function() {
            
        }
        
    });
    
    TK.Projectile = Projectile;
    
}(window.Tilekit));