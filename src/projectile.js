// Projectile
// -------------------------------------------------- //

(function(TK) {
    
    var Projectile = TK.Entity.extend({
        
        initialize: function(options) {

        }
        
    });
    
    TK.Projectile = Projectile;
    
}(window.Tilekit));